export function NavBar() {
    return (
        <nav className="flex align-center">
            <a href="#">home</a>
            <a href="#">about</a>
            <a className="contact-btn" href="#">contact us</a>
        </nav>
    )
}